<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= recipe ? recipe.description || 'Resep masakan lengkap dengan bahan dan cara membuat' : 'Detail resep masakan' %>">
    <meta name="keywords" content="resep, masakan, <%= recipe ? recipe.title : 'resep masakan' %>, bahan masakan, cara memasak">
    <meta name="author" content="Cookpad Search">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="article">
    <meta property="og:title" content="<%= recipe ? recipe.title : 'Resep Masakan' %>">
    <meta property="og:description" content="<%= recipe ? recipe.description || 'Resep lengkap dengan bahan dan cara membuat' : 'Detail resep masakan' %>">
    <% if (recipe && recipe.imageUrl) { %>
    <meta property="og:image" content="<%= recipe.imageUrl %>">
    <% } %>
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:title" content="<%= recipe ? recipe.title : 'Resep Masakan' %>">
    <meta property="twitter:description" content="<%= recipe ? recipe.description || 'Resep lengkap dengan bahan dan cara membuat' : 'Detail resep masakan' %>">
    
    <title><%= recipe ? recipe.title : 'Detail Resep' %> - Pencarian Resep Cookpad</title>
    
    <!-- Favicon -->
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🍳</text></svg>">
    <link rel="apple-touch-icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🍳</text></svg>">
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Alpine.js for animations -->
    <script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"></script>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        
        body {
            font-family: 'Poppins', sans-serif;
            scroll-behavior: smooth;
        }
        
        .fade-in {
            animation: fadeIn 0.6s ease-in;
        }
        
        .slide-up {
            animation: slideUp 0.6s ease-out;
        }
        
        .stagger-item {
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 0.5s forwards;
        }
        
        .ingredient-item {
            opacity: 0;
            transform: translateX(-10px);
            animation: slideInRight 0.4s forwards;
        }
        
        .step-item {
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 0.5s forwards;
        }
        
        .pulse-soft {
            animation: pulseSoft 2s infinite;
        }
        
        .hover-lift {
            transition: all 0.3s ease;
        }
        
        .hover-lift:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
        }
        
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }
        
        @keyframes slideUp {
            from { 
                opacity: 0;
                transform: translateY(30px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes fadeInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        @keyframes slideInRight {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
        
        @keyframes pulseSoft {
            0% { transform: scale(1); }
            50% { transform: scale(1.02); }
            100% { transform: scale(1); }
        }
        
        .bg-gradient-food {
            background: linear-gradient(135deg, #f6d365 0%, #fda085 100%);
        }
        
        .print-hidden {
            display: block;
        }
        
        @media print {
            .print-hidden {
                display: none !important;
            }
            
            body {
                font-size: 12pt;
                background: white;
                color: black;
            }
            
            .bg-white {
                background: white !important;
                box-shadow: none !important;
            }
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Header -->
    <header class="bg-gradient-food text-white shadow-md print-hidden">
        <div class="container mx-auto px-4 py-4">
            <div class="flex items-center justify-between">
                <a href="/" class="inline-flex items-center text-white hover:text-white/90 transition-colors font-medium">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-1" viewBox="0 0 20 20" fill="currentColor">
                        <path fill-rule="evenodd" d="M9.707 14.707a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414l4-4a1 1 0 011.414 1.414L7.414 9H15a1 1 0 110 2H7.414l2.293 2.293a1 1 0 010 1.414z" clip-rule="evenodd" />
                    </svg>
                    Kembali ke Pencarian
                </a>
                
                <button onclick="window.print()" class="inline-flex items-center bg-white text-orange-500 px-4 py-2 rounded-lg hover:bg-gray-100 transition-colors font-medium">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 17h2a2 2 0 002-2v-4a2 2 0 00-2-2H5a2 2 0 00-2 2v4a2 2 0 002 2h2m4 4h6a2 2 0 002-2v-4a2 2 0 00-2-2h-6a2 2 0 00-2 2v4a2 2 0 002 2z" />
                    </svg>
                    Cetak Resep
                </button>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <% if (error) { %>
            <div class="max-w-2xl mx-auto bg-red-50 border border-red-200 text-red-700 px-6 py-4 rounded-xl mb-8 fade-in">
                <div class="flex items-start">
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-3 flex-shrink-0" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <p>Error: <%= error %></p>
                </div>
            </div>
        <% } %>

        <% if (recipe) { %>
            <div x-data="{ activeTab: 'bahan' }" class="max-w-4xl mx-auto">
                <!-- Recipe Header -->
                <div class="bg-white rounded-xl shadow-md overflow-hidden slide-up pulse-soft">
                    <% if (recipe.imageUrl) { %>
                        <div class="relative h-72 md:h-80 overflow-hidden">
                            <img 
                                src="<%= recipe.imageUrl %>" 
                                alt="<%= recipe.title %>" 
                                class="w-full h-full object-cover transition-transform duration-700 hover:scale-105"
                                onerror="this.src='data:image/svg+xml;charset=utf-8,%3Csvg xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22 width%3D%22800%22 height%3D%22400%22 viewBox%3D%220 0 800 400%22%3E%3Crect fill%3D%22%23f3f4f6%22 width%3D%22800%22 height%3D%22400%22%2F%3E%3Ctext fill%3D%22%239ca3af%22 font-family%3D%22sans-serif%22 font-size%3D%2230%22 dy%3D%22.35em%22 text-anchor%3D%22middle%22 x%3D%22400%22 y%3D%22200%22%3EGambar Tidak Tersedia%3C%2Ftext%3E%3C%2Fsvg%3E'"
                            >
                            <div class="absolute inset-0 bg-black/20"></div>
                            <div class="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/70 to-transparent p-6 text-white">
                                <h1 class="text-3xl md:text-4xl font-bold mb-2"><%= recipe.title %></h1>
                                
                                <% if (recipe.description) { %>
                                    <p class="text-white/90 italic"><%= recipe.description %></p>
                                <% } %>
                            </div>
                        </div>
                    <% } else { %>
                        <div class="w-full h-64 bg-gray-100 flex items-center justify-center rounded-t-xl">
                            <span class="text-gray-400 text-lg">Gambar tidak tersedia</span>
                        </div>
                    <% } %>
                    
                    <!-- Recipe Meta Information -->
                    <div class="p-6">
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 mb-6">
                            <% if (recipe.author) { %>
                                <div class="bg-orange-50 p-4 rounded-lg hover-lift">
                                    <h3 class="font-semibold text-gray-700 flex items-center">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                                        </svg>
                                        Oleh
                                    </h3>
                                    <p class="text-gray-600 mt-1"><%= recipe.author.name || recipe.author %></p>
                                </div>
                            <% } %>
                            
                            <% if (recipe.prepTime) { %>
                                <div class="bg-orange-50 p-4 rounded-lg hover-lift">
                                    <h3 class="font-semibold text-gray-700 flex items-center">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                        </svg>
                                        Waktu Memasak
                                    </h3>
                                    <p class="text-gray-600 mt-1"><%= recipe.prepTime %></p>
                                </div>
                            <% } %>
                            
                            <% if (recipe.servings) { %>
                                <div class="bg-orange-50 p-4 rounded-lg hover-lift">
                                    <h3 class="font-semibold text-gray-700 flex items-center">
                                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
                                        </svg>
                                        Porsi
                                    </h3>
                                    <p class="text-gray-600 mt-1"><%= recipe.servings %></p>
                                </div>
                            <% } %>
                        </div>
                        
                        <% if (recipe.datePublished) { %>
                            <div class="text-sm text-gray-500 flex flex-wrap items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                </svg>
                                <span>Diterbitkan: <%= new Date(recipe.datePublished).toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                                
                                <% if (recipe.dateModified) { %>
                                    <span class="mx-2">•</span>
                                    <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" />
                                    </svg>
                                    <span>Diupdate: <%= new Date(recipe.dateModified).toLocaleDateString('id-ID', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                                <% } %>
                            </div>
                        <% } %>
                    </div>
                </div>
                
                <!-- Tab Navigation -->
                <div class="flex border-b border-gray-200 mt-8 mb-6 print-hidden">
                    <button 
                        @click="activeTab = 'bahan'" 
                        :class="activeTab === 'bahan' ? 'text-orange-500 border-b-2 border-orange-500' : 'text-gray-500 hover:text-gray-700'" 
                        class="py-3 px-6 font-medium flex items-center"
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
                        </svg>
                        Bahan-bahan
                    </button>
                    
                    <button 
                        @click="activeTab = 'cara'" 
                        :class="activeTab === 'cara' ? 'text-orange-500 border-b-2 border-orange-500' : 'text-gray-500 hover:text-gray-700'" 
                        class="py-3 px-6 font-medium flex items-center"
                    >
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 mr-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                        </svg>
                        Cara Membuat
                    </button>
                </div>
                
                <!-- Ingredients Section -->
                <div x-show="activeTab === 'bahan'" class="fade-in">
                    <div class="bg-white rounded-xl shadow-md overflow-hidden p-6 mb-8">
                        <h2 class="text-2xl font-semibold text-gray-800 mb-6 flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01" />
                            </svg>
                            Bahan-bahan
                        </h2>
                        
                        <% if (recipe.ingredients && recipe.ingredients.length > 0) { %>
                            <ul class="space-y-3">
                                <% recipe.ingredients.forEach((ingredient, index) => { %>
                                    <li class="ingredient-item text-gray-700 flex items-start" style="animation-delay: <%= index * 0.05 %>s">
                                        <span class="inline-flex items-center justify-center w-6 h-6 bg-orange-100 text-orange-700 rounded-full mr-3 flex-shrink-0 mt-1">
                                            <%= index + 1 %>
                                        </span>
                                        <span><%= ingredient %></span>
                                    </li>
                                <% }); %>
                            </ul>
                        <% } else { %>
                            <p class="text-gray-500">Tidak ada informasi bahan.</p>
                        <% } %>
                    </div>
                </div>
                
                <!-- Steps Section -->
                <div x-show="activeTab === 'cara'" class="fade-in">
                    <div class="bg-white rounded-xl shadow-md overflow-hidden p-6">
                        <h2 class="text-2xl font-semibold text-gray-800 mb-6 flex items-center">
                            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 mr-2 text-orange-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" />
                            </svg>
                            Cara Membuat
                        </h2>
                        
                        <% if (recipe.steps && recipe.steps.length > 0) { %>
                            <ol class="space-y-6">
                                <% recipe.steps.forEach((step, index) => { %>
                                    <li class="step-item bg-gray-50 p-5 rounded-lg" style="animation-delay: <%= index * 0.1 %>s">
                                        <div class="flex items-start">
                                            <span class="inline-flex items-center justify-center w-8 h-8 bg-orange-500 text-white rounded-full mr-4 flex-shrink-0 text-lg font-semibold">
                                                <%= index + 1 %>
                                            </span>
                                            <div class="flex-1">
                                                <p class="text-gray-700"><%= step.text %></p>
                                                
                                                <% if (step.images && step.images.length > 0) { %>
                                                    <div class="mt-4">
                                                        <img 
                                                            src="<%= step.images[0] %>" 
                                                            alt="Langkah <%= index + 1 %>" 
                                                            class="rounded-lg max-w-full md:max-w-xs shadow-md"
                                                            onerror="this.style.display='none'"
                                                        >
                                                    </div>
                                                <% } %>
                                            </div>
                                        </div>
                                    </li>
                                <% }); %>
                            </ol>
                        <% } else { %>
                            <p class="text-gray-500">Tidak ada informasi cara membuat.</p>
                        <% } %>
                    </div>
                </div>
                
                <!-- Print Section (hidden on screen) -->
                <div class="hidden print:block">
                    <h2 class="text-2xl font-semibold text-gray-800 mt-8 mb-4">Bahan-bahan</h2>
                    <% if (recipe.ingredients && recipe.ingredients.length > 0) { %>
                        <ul class="list-disc list-inside mb-8">
                            <% recipe.ingredients.forEach((ingredient) => { %>
                                <li class="text-gray-700 mb-2"><%= ingredient %></li>
                            <% }); %>
                        </ul>
                    <% } %>
                    
                    <h2 class="text-2xl font-semibold text-gray-800 mb-4">Cara Membuat</h2>
                    <% if (recipe.steps && recipe.steps.length > 0) { %>
                        <ol class="list-decimal list-inside">
                            <% recipe.steps.forEach((step) => { %>
                                <li class="text-gray-700 mb-4"><%= step.text %></li>
                            <% }); %>
                        </ol>
                    <% } %>
                </div>
            </div>
        <% } %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-8 mt-12 print-hidden">
        <div class="container mx-auto px-4">
            <div class="text-center">
                <p>&copy; 2023 Pencarian zyann. Semua hak cipta dilindungi.</p>
                <p class="text-gray-400 text-sm mt-2">Website ini menggunakan API dari Cookpad untuk menampilkan resep-resep masakan.</p>
            </div>
        </div>
    </footer>

    <!-- JSON-LD Structured data -->
    <% if (recipe) { %>
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "Recipe",
        "name": "<%= recipe.title %>",
        "description": "<%= recipe.description || 'Resep masakan lengkap' %>",
        "author": {
            "@type": "Person",
            "name": "<%= recipe.author.name || recipe.author || 'Unknown' %>"
        },
        <% if (recipe.imageUrl) { %>
        "image": "<%= recipe.imageUrl %>",
        <% } %>
        "recipeYield": "<%= recipe.servings || '1' %>",
        "recipeIngredient": <%= JSON.stringify(recipe.ingredients || []) %>,
        "recipeInstructions": [
            <% if (recipe.steps) { %>
                <% recipe.steps.forEach((step, index) => { %>
                {
                    "@type": "HowToStep",
                    "text": "<%= step.text %>",
                    "position": "<%= index + 1 %>"
                }<%= index < recipe.steps.length - 1 ? ',' : '' %>
                <% }); %>
            <% } %>
        ],
        "prepTime": "<%= recipe.prepTime || 'PT30M' %>",
        "datePublished": "<%= recipe.datePublished %>",
        "dateModified": "<%= recipe.dateModified || recipe.datePublished %>"
    }
    </script>
    <% } %>
</body>
</html>